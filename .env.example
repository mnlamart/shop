LITEFS_DIR="/litefs/data"
DATABASE_PATH="./prisma/data.db"
DATABASE_URL="file:./data.db?connection_limit=1"
CACHE_DATABASE_PATH="./other/cache.db"
SESSION_SECRET="super-duper-s3cret"
HONEYPOT_SECRET="super-duper-s3cret"
RESEND_API_KEY="re_blAh_blaHBlaHblahBLAhBlAh"
SENTRY_DSN="your-dsn"

# this is set to a random value in the Dockerfile
INTERNAL_COMMAND_TOKEN="some-made-up-token"

# the mocks and some code rely on these two being prefixed with "MOCK_"
# if they aren't then the real github api will be attempted
GITHUB_CLIENT_ID="MOCK_GITHUB_CLIENT_ID"
GITHUB_CLIENT_SECRET="MOCK_GITHUB_CLIENT_SECRET"
GITHUB_TOKEN="MOCK_GITHUB_TOKEN"
GITHUB_REDIRECT_URI="https://example.com/auth/github/callback"

# set this to false to prevent search engines from indexing the website
# default to allow indexing for seo safety
ALLOW_INDEXING="true"

# Tigris Object Storage (S3-compatible) Configuration
AWS_ACCESS_KEY_ID="mock-access-key"
AWS_SECRET_ACCESS_KEY="mock-secret-key"
AWS_REGION="auto"
AWS_ENDPOINT_URL_S3="https://fly.storage.tigris.dev"
BUCKET_NAME="mock-bucket"

# Stripe Payment Processing
# Get your test keys from: https://dashboard.stripe.com/test/apikeys
# Test keys start with sk_test_ (secret key) and work in Stripe test mode
# Production keys start with sk_live_ (never commit production keys!)
STRIPE_SECRET_KEY=sk_test_your_test_secret_key_here
# Webhook secret starts with whsec_ - get from Stripe Dashboard or Stripe CLI
# Optional for local development (only needed when testing webhooks)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Mondial Relay API1 Credentials (for pickup point search and tracking)

# API1
MONDIAL_RELAY_API1_URL=https://api.mondialrelay.com/WebService.asmx
MONDIAL_RELAY_API1_STORE_CODE=
MONDIAL_RELAY_API1_PRIVATE_KEY=
MONDIAL_RELAY_API1_BRAND_CODE=TT

# API2
MONDIAL_RELAY_API2_URL=https://connect-api-sandbox.mondialrelay.com/api
MONDIAL_RELAY_API2_BRAND_ID=
MONDIAL_RELAY_API2_LOGIN=
MONDIAL_RELAY_API2_PASSWORD=
MONDIAL_RELAY_API2_CUSTOMER_ID=

# Store information
STORE_NAME=
STORE_ADDRESS1=
STORE_ADDRESS2=  # Optional
STORE_CITY=
STORE_POSTAL_CODE=
STORE_COUNTRY=  # Defaults to FR if not set
STORE_PHONE=
STORE_EMAIL= # Optional